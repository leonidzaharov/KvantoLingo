# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# Legacy SQLite snapshot — больше не используется (миграция на Supabase Postgres).
# Файл остаётся на локальной машине для архива, но в репозиторий не кладём:
# хеши PIN'ов лежат там, в публичной/полупубличной истории им не место.
/dev.db

.vercel

# Референс-копия duolingo-clone и архив старого проекта
duo/
_archive_old_project/

# Локальные настройки Claude Code (не коммитим)
.claude/

# Лекции от Claude — мои объяснения «на почитать», не часть проекта
lectures/
